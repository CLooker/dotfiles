This repo has my dotfiles.  
When I have a machine, I go to github in the browser and download this repo as a zip, and then I run `install.sh`.  
After that, if I'm using `nix-darwin` then I apply changes using an alias `nix-darwin-switch`, and if I'm just using `home-manager`, then I apply changes using `home-manager switch`.
