## move windows start
# fullscreen
f11 [
    "UTM" ~
    * : ~/.config/skhd/window fullscreen
]

# maximize
cmd - up [
    "UTM" ~
    * : ~/.config/skhd/window maximize
]

# left half
cmd - left [
    "UTM" ~
    * : ~/.config/skhd/window left
] 

# right half
cmd - right [
    "UTM" ~
    * : ~/.config/skhd/window right
] 

# center
cmd - down [
    "UTM" ~
    * : ~/.config/skhd/window center
]
## move windows end

# open apps
ctrl + alt - e [ 
    "UTM" ~
    * : open -a 'IntelliJ IDEA Community Edition.app'           # editor
]
ctrl + alt - f [
    "UTM" ~
    * : open ~                                                  # file browser
]
ctrl + alt - t [
    "UTM" ~
    * : open -a ghostty                                         # terminal
]
ctrl + alt - w [
    "UTM" ~
    * : open -a 'Google Chrome'                                 # web browser
]

